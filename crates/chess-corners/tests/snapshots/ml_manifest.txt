# Per-preset corner count for the ChessRefiner::Ml pipelines.
# Refresh with UPDATE_SNAPSHOTS=1 cargo test -p chess-corners --features ml-refiner --test ml_snapshot
ml_multiscale count=36
ml_single_scale count=36
